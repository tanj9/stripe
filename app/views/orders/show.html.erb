<div class="container">
  <div class="grid grid-cols-12 gap-10">
    <div class="col-span-full md:col-span-6"><%= image_tag @order.teddy.photo_url, width: '100%' %></div>
    <div class="col-span-full md:col-span-6">
      <h1 class="text-3lg mb-8">Order #<%= @order.id %> <small><span class="text-black bg-orange-100 border rounded-full px-3 py-2 font-medium ml-4"><%= @order.state %></span></small></h1>
      <ul class="list-none">
        <li><strong>ISSUED ON</strong> <%= @order.created_at.to_date %></li>
        <li><strong>TEDDY</strong> <%= @order.teddy.name %> [<%= @order.teddy_sku %>]</li>
        <li><strong>TOTAL</strong> <%= '%.2f' % @order.amount_cents.fdiv(100) %> €</li>
        </li>
      </ul>
      <p>Estimated delivery in <strong>2 business days</strong></p>
    </div>
  </div>
</div>
